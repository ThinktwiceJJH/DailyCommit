#include <stdio.h>

int main()
{
    printf("hello world\n")

    return 0;
}

// 팝업 창에는 현재 사용할 수 있는 헤더 파일 목록이 표시된다. 글자를 입력할 때 마다 해당 글자에 해당하는 헤더 파일이 선택됨.
// 헤더 파일 뿐 아니라 자료형, 변수, 함수, 구조체 필드 등 C언어의 각종 문법을 입력할 떄도 팝업 창이 표시된다.

//03.31에 진행할 내용 : VS Code의 C, C++ 컴파일 가능하게 셋팅 진행.